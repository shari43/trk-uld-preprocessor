export enum productTypes {
    ULD = "FFA0",
    TRAILER = "FFA1",
    TUG = "FFA2"
};

export enum eventTypes {
    DIRECT = 1,
    INDIRECT = 2,
    MESH = 3
};

export enum requestTypes {
    PROX = 'prox',
    MESH = 'proxencoded'
};